[project]
name = "snake-game"
version = "0.1.0"
description = "A classic Snake game implementation"
requires-python = ">=3.8"
dependencies = [
    "pygame>=2.5.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build" 